@*@model IEnumerable<Budgeter.Models.CodeFirst.BankAccount>

<h2>Accounts</h2>
<hr />
<div class="container">
    <div class="row">
        <div class="col-md-8">
            <div class="panel panel-default">
                <section class="panel-body">
                    <div class="table-responsive">
                        <table class="table table-striped table-hover">
                            <tr>
                                <th>
                                    @Html.DisplayNameFor(model => model.Name)
                                </th>
                                <th>
                                    @Html.DisplayNameFor(model => model.Balance)
                                </th>
                                <th></th>
                            </tr>
                            @foreach (var item in Model)
                            {
                            <tr>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.Name)
                                    </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.Balance)
                                </td>
                                <td>
                                    <button type="button" class="btn btn-sm btn-success" data-toggle="modal" data-target="#myModalEdit">Edit</button>
                                    <button type="button" class="btn btn-sm btn-success" data-toggle="modal" data-target="#myModalDelete">Delete</button>*@
                                    @*@Html.ActionLink("Edit", "Edit", new { id = item.Id }, new { @class = "btn btn-sm btn-success" }) |
                                        @Html.ActionLink("Delete", "Delete", new { id = item.Id }, new { @class = "btn btn-sm btn-success" })*@
                                @*</td>
                            </tr>
                            }
                        </table>
                    </div>
                    <button type="button" class="btn btn-sm btn-success" data-toggle="modal" data-target="#myModalCreate">Create</button>*@
                    @*@Html.ActionLink("Create Account", "Create", null, new { @class = "btn btn-sm btn-success" })*@
                @*</section>
            </div>
        </div>
        <div>
        </div>
    </div>
</div>*@

<!-- Modal for Edit -->
@*<div id="myModalEdit" class="modal fade" tabindex="-1" role="dialog" aria-labelledby="myModalLabel" aria-hidden="true">
    <div class="modal-dialog">
         Modal content
        <div class="panel panel-default">
            <div class="panel-heading">
                <button type="button" class="close" data-dismiss="modal">&times;</button>
                <h4 class="panel-title">Edit Account</h4>
            </div>
            <section class="panel-body">
                <form id="contact-form" action="@Url.Action("Edit","BankAccount")" method="post">

                    @Html.AntiForgeryToken()
                    <div>
                        <label for="text">Name</label>
                        <input type="text" class="form-control" name="Name" />
                    </div>
                    <div>
                        <button class="btn btn-sm btn-danger">Submit</button>
                    </div>
                </form>
            </section>
        </div>
    </div>
</div>*@

<!-- Modal for Delete -->
@*<div id="myModalDelete" class="modal fade" tabindex="-1" role="dialog" aria-labelledby="myModalLabel" aria-hidden="true">
    <div class="modal-dialog">
         Modal content
        <div class="panel panel-default">
            <div class="panel-heading">
                <button type="button" class="close" data-dismiss="modal">&times;</button>
                <h4 class="panel-title">Delete</h4>
            </div>
            <div class="panel-body">
                <form method="Post" action="@Url.Action("Delete","BankAccounts")">
                    @Html.AntiForgeryToken()
                    <h5>Are you sure you want to delete this account?</h5>
                    <input type="submit" value="Delete Account" class="btn btn-sm btn-danger" />
                </form>
            </div>
        </div>

    </div>
</div>*@

<!-- Modal for Create -->
@*<div id="myModalCreate" class="modal fade" tabindex="-1" role="dialog" aria-labelledby="myModalLabel" aria-hidden="true">
    <div class="modal-dialog">
         Modal content
        <div class="panel panel-default">
            <div class="panel-heading">
                <button type="button" class="close" data-dismiss="modal">&times;</button>
                <h4 class="panel-title">Create Account</h4>
            </div>
            <section class="panel-body">
                <form id="contact-form" action="@Url.Action("Create","BankAccounts")" method="post">

                    @Html.AntiForgeryToken()
                    <div>
                        <label for="text">Name</label>
                        <input type="text" class="form-control" name="Name" />
                    </div>
                    <div>
                        <label for="text">Balance</label>
                        <input type="text" class="form-control" name="Name" />
                    </div>
                    <div>
                        <input type="hidden" class="form-control" name="HouseholdId" />
                    </div>
                    <div>
                        <button class="btn btn-sm btn-success">Create</button>
                    </div>
                    <p class="text-success ">@ViewBag.SuccessMessage</p>
                </form>
            </section>
        </div>
    </div>
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}*@




@model IEnumerable<Budgeter.Models.CodeFirst.BankAccount>
<h3>Accounts</h3>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Balance)
        </th>
        <th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Balance)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.Id }) |
                @Html.ActionLink("Details", "Details", new { id = item.Id }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.Id })
            </td>
        </tr>
    }

</table>